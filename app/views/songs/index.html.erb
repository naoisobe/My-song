<div class="search">
  <%= render 'layouts/search' %>
</div>
<div class="container-fluid">
  <div class="column-select-box">
    <a href="<%= songs_path %>" class="<%= "column-type" if current_page?(songs_path)%>"><h4>新着順</h4></a>
    <a href="<%= songs_rank_path %>" class="<%= "column-type" if current_page?(songs_rank_path)%>"><h4>ランキング</h4></a>
  </div>
  <div class="row contents">
    <% @song.each do |song| %>
      <%= link_to song_path(song) do %>
        <div class="col-xl-3 col-lg-4 col-md-4 col-sm-6">
          <div class="song-card">
            <%= image_tag song.thumbnail.to_s, class:"thumbnail" %>
            <div class="card-body">
              <h5><%= song.title %></h5>
                <%= link_to user_path(song.user) do %>
                  <div class="user-card">
                    <div><%= image_tag song.user.profile_image.to_s, class:"post-profile" %></div>
                    <p class="user-name over-string"><%= song.user.name %></p>
                  </div>
                <% end %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
